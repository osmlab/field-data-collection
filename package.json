{
  "name": "field-data-collection",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules $TMPDIR/react-* && watchman watch-del-all && npm cache clean && yarn",
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "ios": "react-native run-ios && react-native log-ios",
    "android": "react-native run-android && react-native log-android"
  },
  "dependencies": {
    "react": "15.4.2",
    "react-native": "0.39.2",
    "react-native-mapbox-gl": "github:alseageo/react-native-mapbox-gl#queryRenderedFeatures-android",
    "react-navigation": "^1.0.0-beta.9"
  },
  "devDependencies": {
    "babel-preset-react-native": "1.9.1"
  }
}
